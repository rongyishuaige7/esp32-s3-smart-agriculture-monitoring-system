# 验证记录与真机复测清单

## 当前验证口径

本文件区分三层事实：静态公开门禁、隔离构建和当前公开 commit 真机复测。GitHub 仓库可见性、默认分支、HEAD、Actions 结论和构建产物数量必须实时回读；不能从候选目录或旧日志推断。

| 检查项 | 当前状态 | 证据边界 |
| :-- | :-- | :-- |
| 公开范围扫描 / 结构检查 / 源码契约 | 待本轮最终候选执行并记录 | 只检查公开文件与受检文本/结构 |
| .NET 8 restore/build | 待本轮最终候选执行并记录 | 不等于桌面 UI、账户、网络或真实数据已验证 |
| ESP32-S3 PlatformIO 三环境编译 | 待本轮最终候选执行并记录 | 不等于烧录、GPIO、传感器、OLED、Wi-Fi、TCP、执行器或电气安全 |
| 当前 `main` exact-SHA GitHub Actions | 必须从 Actions 页面/API 回读 | 只计 completed / success；不替代真机 |
| 当前公开 commit 真机复测 | **未执行** | 没有日期化、可回读的实物证据 |

## 本地发布门禁

```bash
bash scripts/verify.sh
```

脚本在临时目录中构建，不保留 `.NET`/PlatformIO 生成物在候选树。它不烧录设备、不连接 Wi-Fi/TCP、不启动桌面应用或任何服务、不访问真实传感器、不连接执行器。

## 当前 commit 真机复测（尚未执行）

公开 commit 固定后，才能逐项记录日期、完整 commit、执行者、精确板型、模块、供电/电平、接线、输入条件、结果、失败/偏差和脱敏媒体状态：

1. ESP32-S3 板型、USB/低压供电、烧录、启动、复位；
2. DHT11、BH1750、ACD10、BMP280、OLED 的地址/读数/拔除失败路径；不得写成作物、空气质量或安全结论；
3. 默认 GPIO5/GPIO6/GPIO38 输入路径，及受监督低压台架的外部驱动复核；不得 GPIO 直驱负载；
4. Wi-Fi/TCP opt-in 的可信隔离网络、断网和失败路径；不得声称认证、加密、可靠送达或远程控制；
5. 公开桌面示例数据界面的启动、加载、边界提示和异常路径；不得把示例数据写成真实设备、网络、账号、数据库或持久化能力；
6. 断电、模块拔除、配置缺失、重启与异常路径；
7. 实物照片、视频、接线/PCB 资料的权属、脱敏和 EXIF/GPS 复核。

未完成相应项目之前，README、GitHub、Hardware Lab 和 Profile 必须保持“当前真机复测：未执行”。
